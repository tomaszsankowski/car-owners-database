import { PersonDetails } from './person-details.model';

describe('PersonDetails', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new PersonDetails()).toBeTruthy();
  });
});
